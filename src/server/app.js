const express = require('express');
// Start up an instance of app
const app = express();

// please remove the comments in the previous code lines:(7-9) when you need test it 

// app.get("/", (req, res) => {
//   res.status(200).send("Hello World!");
// });

module.exports = app 

